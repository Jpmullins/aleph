2026-08-29T16:14:14Z

# The moment the deployed stack started recording model calls with BOTH a price
# and a run to attribute them to.
#
# It took two deploys, and the gap between them is instructive. The first
# (01:01) landed WS-MEP-1 and priced the calls — `pricing_source` went from
# `unknown` on 100% of assistant traffic to `gateway`. It did NOT attribute
# them: `awrap_model_call` read the run id from `runtime.config`, and LangGraph's
# `Runtime` has no `config` at all. Its own docstring says so and points at
# `langgraph.config.get_config()`; `ToolRuntime` adds one, which is why the TOOL
# events were attributed correctly in the same deploy while the MODEL calls were
# not. Reading a channel that does not carry the key is the same mistake as
# reading `metadata`, made twice, two hours apart.
#
# The second deploy (01:04) reads `get_config()`. `assistant.turn` and
# `assistant.subagent.*` rows since carry `pricing_source=gateway` and a
# non-null `agent_run_id`.
#
# It took a THIRD (01:30), and the probe found it rather than a reader. Driving
# real turns produced 13 attributed rows next to 9 orphans:
# `assistant.corpus_search.query_embed`, `assistant.page_selection` and
# `assistant.compose` — all three from inside ONE tool. `search_knowledge`
# called the retrieval router with `agent_run_id=None`, and the router makes
# three further model calls of its own. The agent middleware never sees them:
# it wraps the ORCHESTRATOR's model call, and these happen a layer down.
#
# Three deploys to attribute one turn, each fixing a real and different gap,
# and the cutoff is the last of them because that is when the claim "every
# model call a turn makes belongs to that turn" first became true.
#
# A FOURTH (14:00), and this one was not on the agent path at all.
# `aleph_evals.retrieval_eval` built its own `LiteLLMClient` with a bare
# `PricingTable()` to drive the reranker. An empty table knows no rates, so all
# 90 `rks.rerank.llm` rows from two measurement runs landed
# `pricing_source="unknown"`, `cost_usd=0` — a measurement harness reporting its
# own spend as free, into the same ledger the number counts. The eval now builds
# its table with `PricingTable.from_discovery(...)`; the 45 rows since are
# `gateway`-priced.
#
# The general fix matters more than the specific one: `LiteLLMClient` now warns
# at CONSTRUCTION when its pricing table is empty, naming the consequence. It is
# a warning and not an error because `copilot_cost_callback` legitimately falls
# back to an empty table before the kernel binds one — but a caller who meant to
# pass rates and passed `PricingTable()` used to learn about it from a health
# number two days later.
#
# `docs/plan.md` number 5 is measured FORWARD from here. `docs/decisions.md` D9
# says why: the 159 rows before it are true records of calls that really were
# made while the mechanism was broken, and editing an append-only ledger to make
# a number look better is the first exception that there is never only one of.
#
# A cutoff in the FUTURE makes the number unfalsifiable, so
# `scripts/_acceptance/status_numbers.py` refuses to report green on one.
#
# A FIFTH (2026-08-25 00:39), and this one was a measurement bug rather than a
# missing record: the discriminator could not tell the agent path from a
# diagnostic one.
#
# `uncosted_model_calls` counts `agent_run_id IS NULL AND purpose LIKE
# 'assistant%'`, on the reasoning stated above that "the prefix is the honest
# discriminator — `purpose` is what the call site declares itself to be". It was
# not the call site. `WikiFirstRetrievalRouter` wrote the purpose itself, so a
# real agent turn and the debug-only `/retrieval/debug` route emitted identical
# strings. Ten rows on this instance were counted as agent-path attribution
# defects that did not exist, while the agent path was in fact 13/13 attributed
# over the same window. The router takes a `purpose_prefix` now and the debug
# route declares itself `diagnostic`, which has no turn to belong to and never
# will — requiring one would mean minting a fake run so a number could go green.
#
# The other seven rows in that count were never real spend either: four are
# `zebra-mini`/`zebra-LARGE`, synthetic model names from an audit probe, which
# have no price because they do not exist; three are `assistant.turn.failed`
# against a model this gateway does not serve, recorded with 0 input tokens,
# 0 completion tokens and $0 — a call that failed before consuming anything.
#
# Moved rather than backfilled, per D9, and moved only after measuring forward
# rather than assuming: 143 model calls were recorded between this timestamp and
# the run that checked it, and ZERO of them are unpriced or unattributed. An
# empty window would have proved nothing; a busy clean one is the evidence.
#
# `uncosted_model_calls_legacy` still prints the all-time figure beside it, so
# what this timestamp forgives stays visible on the same screen.

# A SIXTH (2026-08-29T16:14:14Z), and this one was a channel that a new caller did
# not fill rather than a mechanism that was broken.
#
# The three rows this forgives are `assistant.corpus_search.query_embed`,
# `assistant.page_selection` and `assistant.compose` at 2026-08-25 19:48 — the
# same three purposes the THIRD deploy above fixed, from the same tool, orphaned
# again by a path that did not exist when that fix was written.
# `delegated_subagent_job` built its graph config with `projectId` and nothing
# else, so a DELEGATED `read_wiki_deeply` drove the retrieval router with
# `run_id_from_config` returning None. The API path was never affected; only
# delegation was, and delegation was three days old.
#
# The lesson is the one the third deploy already stated and did not generalise:
# attribution travels in `configurable`, so every NEW way of entering the graph
# has to fill it. The fix threads the delegation's own `AgentRun` id — a row
# that existed the whole time — and
# `test_delegation_job.py::test_the_delegated_graph_config_carries_the_run_id`
# pins the config's shape rather than the intent, because every other test in
# that file stubs `_run_subagent` and so replaced the function that had the bug.
#
# Measured forward before moving, per D9. THREE delegated retriever runs plus
# the acceptance gate's own chat and bootstrap traffic: **20 model calls across
# 8 distinct purposes** — `assistant.turn`, the three router purposes above,
# `rks.rerank.llm`, `bootstrap.scope`, `research.plan`, `research.triage` — with
# ZERO unattributed on the agent path and ZERO unpriced.
#
# That window is smaller than the 143 the fifth move used, and the difference is
# worth stating rather than glossing: this is not a broad sample, it is a
# targeted one. Every one of the three purposes that was orphaned is present,
# from the exact path that orphaned them, attributed 3 times out of 3. Breadth
# of paths is what makes it evidence; volume is what it lacks.
