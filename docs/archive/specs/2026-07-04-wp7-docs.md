# WP-7 — Docs reset + F1–F7 finale

**Date:** 2026-07-04 · **Package:** WP-7 of `GOAL.md` · **Proves:** Final State F6 (+ carries the
whole-goal F1–F7 finale). **Status:** implementing

## Problem

`docs/` (80 files) still describes the pre-WP system: AIQ as the live research subsystem,
MinIO-by-default, presigned URLs, the pre-WP-4 workspace, orphaned connectors, the old increment/
wave history. CLAUDE.md and README.md make dozens of now-false claims. WP-7 resets the docs to
describe only the system that exists, then performs the F7 end-to-end demo and displays the
completed F1–F6 checklist.

## Design — the doc inventory

### Archive (move verbatim, preserve provenance)

`git mv` under a new `docs/archive/`:
- `docs/superpowers/` → `docs/archive/superpowers/` (all specs + plans + excalidraws — the whole
  pre-WP history).
- `docs/system-assessment.md` → `docs/archive/` (stale 2026-06-30 review).
- Every subsystem/engineering/ops/security/domain/agents/a2ui/retrieval/pipelines/wiki/
  connectors/reviewers/hypotheses/datasets/artifacts/evals/ui doc → `docs/archive/subsystem/`
  (superseded by the fresh 7-doc set).
- The strays `docs/goals/`, `docs/template_projects/` → `docs/archive/`.
- The six current WP specs `docs/specs/2026-07-*` → `docs/archive/specs/` **after** the fresh
  docs are drafted from them (they are the writing source; they stay reachable as the detailed
  record).

**Survives at `docs/` top level:** `implementation-log.md` (append-only — the canonical shipped
record) + the seven new docs.

### The fresh doc set (each ≤ ~200 lines, written from finished code)

| Doc | Source of truth |
|-----|-----------------|
| `architecture.md` | the final `pyproject.toml` package list + DAG; the load-bearing rules (amended rule 8); the three committed sweeps as living invariants; process boundaries (api / workers / code-runner / copilot-runtime — no aiq-server). |
| `research-loop.md` | WP-3 spec (native `aleph-research` `deep_research_job`, tool binding/allowlist, `/synthesize` re-target) + WP-2 (scholar tools, Consensus, reviewer citation pass). |
| `workspace.md` | WP-4 spec (data-bound surfaces + delta substrate, reader tier, sandbox `code_runner`, agent eyes+hands, the catalog roster + the two workspace sweeps). |
| `wiki.md` | WP-6 spec (freshness math, refresh job + ApprovalCards, retraction state machine + blast-radius, drift) + the wiki-first retrieval + curator. |
| `storage.md` | WP-1 spec (fs-default `AssetStore` protocol, key layout, the one streaming route, `s3` opt-in profile, security posture). |
| `operations.md` | `bootstrap-local.sh` + `verify-gateway.sh` + compose (fs default, `s3` profile, no MinIO/aiq) + the gate suite. |
| `security.md` | the Security-posture section of every WP spec (auth modes, agent tokens, credential encryption, code_runner isolation, CSP-sandbox asset serving) + the documented deferred **SSE×OIDC token-transport gap** (GOAL out-of-scope). |

### CLAUDE.md rewrite (outline)

Rewrite so every claim matches WP-1..6: the intro paragraph (native research loop, not AIQ);
the package list (add `aleph-scholar`, `aleph-research`, `aleph-code-runner`; remove
`aleph-aiq`); the connectors note (wired into the native loop, not orphaned); rule 3 (drop AIQ
clauses); **rule 8 amended** (agent code runs only in the sandboxed `code_runner`; interactive
artifacts render only in sandbox iframes; no agent-emitted SQL); process boundaries (replace the
aiq-server bullet with the `aleph-research` worker + `deep_research_job` + re-targeted
`/synthesize`; add the `code_runner` service); the commands (delete the NGC/nvcr line; fix the
endpoints — no aiq :8001, MinIO only under the `s3` profile); the Docs map (point at the fresh
7-doc set + impl-log + `docs/archive/`); drop the `~/code/aiq` reference; add the sweeps as
enforced invariants.

### README.md

Fix the core-loop description (native research, not AIQ), the endpoints (drop aiq :8001; MinIO
opt-in), reconcile the package layout.

### Drift-prevention

- `scripts/check-docs-drift.sh` (committed, CI-wired): asserts
  `grep -rniE "aiq|minio" docs CLAUDE.md README.md` hits ONLY `docs/archive/` and
  `docs/implementation-log.md` (historical) — the F6 acceptance grep, enforced.
- `scripts/check-claude-commands.sh` (committed, CI-wired): extracts the shell commands from
  CLAUDE.md's command blocks and asserts each references only paths/scripts/targets that exist
  in the repo (a fresh-clone executability guard — it does not run docker, but verifies every
  referenced script/file/compose-service exists and no command names a deleted thing).

## Final State (falsifiable) — F6 verbatim

1. **`docs/` describes only the system that exists.** Verify: old specs/assessments/audit live
   under `docs/archive/`; `implementation-log.md` survives append-only; the fresh 7-doc set
   (architecture, research-loop, workspace, wiki, storage, operations, security, each ≤~200
   lines) exists and is written from the finished code; CLAUDE.md rewritten (no AIQ, no
   MinIO-by-default, amended rule 8, the sweeps as living invariants).
2. **A fresh review agent given only the new docs finds zero claims contradicted by code.**
   Verify: a fresh subagent reads only the 7 new docs + CLAUDE.md + README.md, checks each
   load-bearing claim against the code, and reports zero contradictions (report appended to the
   impl-log).
3. **Every command in CLAUDE.md executes on a fresh clone.** Verify: `scripts/
   check-claude-commands.sh` passes (every referenced script/file/target exists; no deleted
   thing named).
4. **`grep -rni "aiq\|minio" docs CLAUDE.md README.md` hits only `docs/archive/` and historical
   impl-log entries.** Verify: `scripts/check-docs-drift.sh` passes.

## The F7 finale (performed in-browser, logged step-by-step in the impl-log)

Project create → URL + PDF ingest → Library viewers render via the streaming route → "research
this topic" → scholar tools + native loop visibly run in Activity → proposal in Briefs → approve
→ curator-linked wiki page with freshness badge → "summarize this page" with the page merely
open (shared state) → agent pins a sandbox-generated chart to Briefs (code_runner → artifact →
iframe) → a lit-review question answered with verified DOIs → retract a cited source → the wiki
page shows the retraction marker and the brief shows `drifted`. Each step + its evidence logged.

## Definition of done (whole goal)

Every F1–F6 statement verified with displayed evidence; the F7 demo performed in-browser and
logged; §Progress shows all seven packages closed; each package closed through a displayed
adversarial review reporting zero violations.
